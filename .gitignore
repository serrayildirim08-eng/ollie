node_modules/
dist/
build/
.DS_Store
.env
.env.local
.env.*.local
*.log
.vite/
.turbo/
.cache/
coverage/
.wrangler/
.idea/
.vscode/
*.tsbuildinfo

# Supabase CLI link state
supabase/.temp/

# Stray empty leftover from CLI link
workers/cron/supabase/

# 3D model source files — optimized .glb's ship from apps/{web,ios}/{public,www}/models/
# Originals live elsewhere (Meshy AI / cloud storage), too large for git.
assets/*.glb
assets/*/*.glb

# Claude Code agent state (worktrees, sessions, cache — multi-GB, never ship)
.claude/

# Cofounder/pitch presentation artifacts (regenerated, not source of truth)
OLLIE_COFOUNDER_PRESENTATION.*
