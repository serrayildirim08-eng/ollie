/**
 * Partner module · public surface.
 *   - PartnerBox  → the /box/partner screen (pairing + consent + paired view)
 *   - PartnerCard → the Level-2 ambient line for Home
 */

export { PartnerBox } from './PartnerBox';
export { PartnerCard } from './PartnerCard';
